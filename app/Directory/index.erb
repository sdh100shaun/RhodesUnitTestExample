<div class="pageTitle">
  <h1>Directories</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @directories.each do |directory| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => directory.object %>">
            <span class="title"><%= directory.department %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>